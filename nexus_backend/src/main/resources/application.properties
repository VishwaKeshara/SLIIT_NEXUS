spring.application.name=nexus
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/sliit_nexus}

server.servlet.session.cookie.same-site=lax

app.frontend-url=${FRONTEND_URL:http://localhost:5173}
app.security.dev-login-enabled=${DEV_LOGIN_ENABLED:true}

# backend/src/main/resources/application.properties

# Application
spring.application.name=SLIIT_Nexus
server.port=8080

# OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=75013242083-rbbaho0fvl793istgb217kl0eaanjfga.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=email,profile

# JWT Configuration
app.jwt.secret=your-256-bit-secret-key-for-jwt-signing-purposes-only
app.jwt.expiration-ms=86400000

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.sliit.nexus=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# CORS
app.cors.allowed-origins=http://localhost:3000

# Actuator
management.endpoints.web.exposure.include=health,info